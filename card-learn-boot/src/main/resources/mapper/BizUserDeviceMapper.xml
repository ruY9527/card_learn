<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.card.learn.mapper.BizUserDeviceMapper">

    <select id="selectByDeviceToken" resultType="com.card.learn.entity.BizUserDevice">
        SELECT * FROM biz_user_device WHERE device_token = #{deviceToken}
    </select>

</mapper>
