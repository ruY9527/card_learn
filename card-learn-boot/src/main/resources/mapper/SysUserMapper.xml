<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.card.learn.mapper.SysUserMapper">

    <select id="selectByUsername" resultType="com.card.learn.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultType="com.card.learn.entity.SysUser">
        SELECT *
        FROM sys_user
        WHERE email = #{email}
          AND del_flag = '0'
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM sys_user
        WHERE email = #{email}
          AND del_flag = '0'
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM sys_user
        WHERE username = #{username}
          AND del_flag = '0'
    </select>

    <select id="selectPageByCondition" resultType="com.card.learn.entity.SysUser">
        SELECT * FROM sys_user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
